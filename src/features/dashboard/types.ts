export type CandidateDashboard = {
  totalApplications: number;
  pending: number;
  accepted: number;
  rejected: number;
};

export type CompanyDashboard = {
  totalApplications: number;
  pending: number;
  accepted: number;
  rejected: number;
  uniqueJobsWithApplicants: number;
};
